# 🎯 MerchanVision - Configurações de Exemplo
# Copie este arquivo para .env e ajuste os valores conforme necessário

# ========================================
# CONFIGURAÇÕES GERAIS
# ========================================
# Classes do sistema (separadas por vírgula)
CLASSES=break,conteudo,merchan

# Configurações personalizadas podem ser criadas para diferentes contextos
# seguindo o padrão: CONTEXTO_PARAMETRO=valor
# Exemplo: EMPRESA_RF_N_ESTIMATORS=250

# ========================================
# RANDOM FOREST (CLASSIFICADOR PRINCIPAL)
# ========================================
RF_N_ESTIMATORS=180
RF_MAX_DEPTH=14
RF_MIN_SAMPLES_SPLIT=5
RF_MIN_SAMPLES_LEAF=2
RF_CLASS_WEIGHT=balanced
RF_RANDOM_STATE=42

# ========================================
# PROCESSAMENTO DE VÍDEO
# ========================================
VIDEO_RESIZE_WIDTH=224
VIDEO_RESIZE_HEIGHT=224
VIDEO_FPS_EXTRACT=1
VIDEO_FRAMES_PER_VIDEO=5
VIDEO_MAX_DURATION=60
VIDEO_MIN_DURATION=3

# ========================================
# SISTEMA HÍBRIDO
# ========================================
# Pesos da combinação híbrida (devem somar próximo a 1.0)
HYBRID_IMAGE_WEIGHT=0.666
HYBRID_MERCHAN_WEIGHT=0.334

# Boosts para indicadores visuais
MAIN_INDICATOR_BOOST=0.8          # QR-code, telefone, preço
SECONDARY_INDICATOR_BOOST=0.5     # Email, endereço/site
NO_INDICATOR_BOOST=-0.8           # Sem indicadores (boost para conteúdo)

# Thresholds de decisão
HYBRID_CONFIDENCE_THRESHOLD=0.5
HIGH_CONFIDENCE_BYPASS=0.7

# ========================================
# DETECÇÃO DE INDICADORES VISUAIS
# ========================================
# OCR (Tesseract)
OCR_LANGUAGE=por
OCR_ENGINE_MODE=3
OCR_PAGE_SEG_MODE=6
OCR_DENOISE=True
OCR_RESIZE_FACTOR=2.0

# Padrões de detecção (True/False)
DETECT_QR_CODES=True
DETECT_PHONE_NUMBERS=True
DETECT_PRICES=True
DETECT_EMAILS=True
DETECT_ADDRESSES=True

# ========================================
# TEMPO REAL
# ========================================
# Configurações de processamento
REALTIME_WINDOW_SECONDS=3
REALTIME_FPS_TARGET=30
REALTIME_BUFFER_SIZE=90
REALTIME_PROCESS_INTERVAL=1

# Performance vs qualidade
REALTIME_FRAME_SKIP=3
REALTIME_RESIZE_FACTOR=1.0
REALTIME_QUALITY_MODE=balanced    # fast, balanced, quality

# ========================================
# LOGGING E DEBUG
# ========================================
# Nível de log (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
LOG_MODEL_SUMMARY=True
LOG_TRAINING_PROGRESS=True
LOG_PREDICTIONS=False

# ========================================
# CONFIGURAÇÕES AVANÇADAS
# ========================================
# Performance
USE_GPU=True
GPU_MEMORY_GROWTH=True
NUM_WORKERS=4
MULTIPROCESSING=True

# Cache
CACHE_FEATURES=True
FEATURE_CACHE_SIZE=1000
MODEL_CACHE=True

# Regularização (para modelos DNN, se usados no futuro)
L1_REGULARIZATION=0.0001
L2_REGULARIZATION=0.0001
DROPOUT_RATE=0.3

# Data Augmentation (se usado)
AUGMENTATION_ENABLED=False
AUGMENTATION_ROTATION=10
AUGMENTATION_ZOOM=0.1
AUGMENTATION_HORIZONTAL_FLIP=True